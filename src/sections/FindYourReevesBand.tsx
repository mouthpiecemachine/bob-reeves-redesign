import ReevesConfigurator from '../components/ReevesConfigurator'

/**
 * FIND YOUR REEVES — homepage beat 02. The approved configurator renders
 * fully open: the interactive mouthpiece, the three information cards and
 * the buy block. No locked state, no intro card, no extra click.
 */
export default function FindYourReevesBand() {
  return <ReevesConfigurator />
}
